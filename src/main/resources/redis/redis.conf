# RDB snapshot: every 60s if ≥1000 writes
save 60 1000

# AOF: append-only file enabled, fsync every second
appendonly yes
appendfsync everysec

# Data directory (must match your volume mount)
dir /data

# (Redis 7+: subdirectory for AOF segments)
appenddirname appendonlydir